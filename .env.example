# Database mode: postgres (default) or sqlite
DB_DRIVER=postgres

# PostgreSQL settings
PGHOST=localhost
PGPORT=5432
PGDATABASE=rebar_db
PGUSER=rebar_user
PGPASSWORD=your_password_here

# SQLite settings (if DB_DRIVER=sqlite)
DB_PATH=data/app.db
