# Core web framework & server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# File uploads & image handling
python-multipart>=0.0.9
pillow>=10.0.0
opencv-python>=4.10.0              # or opencv-python-headless for lighter install

# Model inference (ONNX runtime)
onnxruntime>=1.19.0                 # use onnxruntime-gpu if you have NVIDIA GPU + CUDA

# Clustering / post-processing
scikit-learn>=1.5.0

# Database (PostgreSQL)
psycopg2-binary>=2.9.9              # binary version – easier on Windows

# OAK camera / DepthAI integration
depthai>=2.28.0

# Excel generation/export
openpyxl>=3.1.0

# Utilities
python-dotenv>=1.0.0                # for .env file loading
numpy<2
hdbscan
scikit-learn                      # required by many above packages

# Optional / if you have any PyTorch fallback or hybrid code
# torch>=2.4.0
# torchvision>=0.19.0